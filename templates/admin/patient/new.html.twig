{% extends 'admin/layout/admin-base.html.twig' %}

{% block title %}
    {{ parent() }} | {{ 'patient.new_title'|trans }}
{% endblock %}

{% block body %}
    <div class="container-lg">
        <h1>{{ 'patient.new_header'|trans }}</h1>
        {{ include('admin/patient/_form.html.twig') }}

        <a href="{{ path('admin_patient_index') }}">{{ 'common.actions.back_to_list'|trans }}</a>
    </div>
{% endblock %}
